# AI뉴스자동화 v3 — 오늘 변경사항 정리

**파일**: [AI뉴스자동화_v3_20260624.json](file:///c:/Github_Folk/News_alert/AI뉴스자동화_v3_20260624.json)  
**작업일**: 2026-06-25

---

## 변경 목적

RSS 피드 수집 실패 시 워크플로우 전체가 중단되는 문제를 해결하여,  
**재시도 → 실패 시 알림 → 나머지 소스로 계속 진행**하도록 개선.

---

## 수정된 노드 (3개)

### 1. `YNA` — httpRequest 노드
| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 재시도 | ❌ 없음 | ✅ 최대 3회 (`retryOnFail: true`) |
| 재시도 간격 | — | 3,000ms |
| 오류 처리 | 워크플로우 중단 | `continueErrorOutput` (오류 분기) |

### 2. `Hankyung` — httpRequest 노드
| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 재시도 | ❌ 없음 | ✅ 최대 3회 |
| 재시도 간격 | — | 3,000ms |
| 오류 처리 | 워크플로우 중단 | `continueErrorOutput` |

### 3. `fnnews` — httpRequest 노드
| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 재시도 | ❌ 없음 | ✅ 최대 3회 |
| 재시도 간격 | — | 3,000ms |
| 오류 처리 | 워크플로우 중단 | `continueErrorOutput` |

### 4. `Deduplicate News` — Code 노드
| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 오류 항목 필터 | ❌ 없음 | ✅ `_rssError` 마커 항목 자동 제거 |

```js
// 추가된 코드 (상단)
const validItems = items.filter(item => !item.json._rssError);
if (validItems.length === 0) return [];
```

---

## 새로 추가된 노드 (4개)

### 5. `YNA 오류 처리` — Code 노드 (신규)
- **위치**: YNA의 오류 출력(error output)에 연결
- **동작**:
  - `console.error()`로 n8n 실행 로그에 기록
  - `_rssError: true` 마커 포함 오류 정보 반환
- **연결**: → Merge Sources (input 0) + → RSS 오류 알림 (Notion)

### 6. `Hankyung 오류 처리` — Code 노드 (신규)
- **위치**: Hankyung의 오류 출력에 연결
- **동작**: 위와 동일 (소스명만 다름)
- **연결**: → Merge Sources (input 1) + → RSS 오류 알림 (Notion)

### 7. `fnnews 오류 처리` — Code 노드 (신규)
- **위치**: fnnews의 오류 출력에 연결
- **동작**: 위와 동일 (소스명만 다름)
- **연결**: → Merge Sources (input 2) + → RSS 오류 알림 (Notion)

### 8. `RSS 오류 알림 (Notion)` — Notion 노드 (신규)
- **자격증명**: 기존 `Notion OAuth2 API` 재사용
- **저장 DB**: `뉴스자동화` (기존과 동일)
- **저장 형식**:

| 필드 | 내용 |
|------|------|
| 제목 | ⚠️ [RSS 오류] 연합뉴스 (YNA) 수집 실패 |
| 요약문 | 3회 재시도 후에도 RSS 피드 수집 실패 (시각) |
| 발행일시 | 오류 발생 시각 (ISO 형식) |
| 언론사 | 시스템 오류 |

---

## 전체 오류 처리 흐름

```
RSS 피드 요청
    ↓ (실패 시 3초 간격으로 최대 3회 재시도)
    ↓ 3회 모두 실패
[YNA/Hankyung/fnnews] 오류 처리 노드
    ├──→ RSS 오류 알림 (Notion) 🔔  오류 내용 DB에 저장
    └──→ Merge Sources              _rssError 마커 전달
              ↓
         Deduplicate News           _rssError 항목 필터링 후 제거
              ↓
         나머지 정상 소스로 계속 진행 ✅
```

---

> [!NOTE]
> 3개 RSS 소스 중 **일부가 실패해도** 나머지 소스의 뉴스로 워크플로우가 정상 완료됩니다.  
> 실패한 소스는 Notion DB에 ⚠️ 표시로 기록되어 추적 가능합니다.
